{
  "name": "gneex-api",
  "version": "1.0.0",
  "private": true,
  "description": "API opcional G-NEEX: bootstrap de usuarios y login contra servidor (la SPA sigue usando localStorage por defecto).",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0"
  }
}
